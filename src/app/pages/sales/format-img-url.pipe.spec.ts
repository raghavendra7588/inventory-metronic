import { FormatImgUrlPipe } from './format-img-url.pipe';

describe('FormatImgUrlPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatImgUrlPipe();
    expect(pipe).toBeTruthy();
  });
});
